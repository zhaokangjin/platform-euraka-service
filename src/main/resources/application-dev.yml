server:
  port: 12000
  context-path: /eureka_server
  compression:
    enabled: true
  tomcat:
    uri-encoding: UTF-8
spring:  
  application: 
    name: eureka.server
eureka: 
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url: 
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
swagger:
    basepackage: com.platflat.eureka.server.provider
    service:
        name: sendinfo product
        description: 产品中心
        developer: admin
logging:
  level:
    com.netflix: warn
    org.springframework.cloud: warn
    com.platform: debug